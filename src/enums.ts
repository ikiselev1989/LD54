export enum SCENES {
	LEVEL = 'level'
}

export enum DIRECTION {
	LEFT = 'left',
	RIGHT = 'right',
	UP = 'up',
	DOWN = 'down'
}

export enum CHARACTER_STATES {
	IDLE = 'idle',
	MOVE = 'move',
	PUNCH = 'punch',
	BLOCK = 'block',
	DAMAGE = 'damage'
}
